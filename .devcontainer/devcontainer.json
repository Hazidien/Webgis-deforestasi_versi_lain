{
  "name": "WebGIS Deforestation",
  "image": "mcr.microsoft.com/devcontainers/python:3.13-bookworm",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "GeoAI Deforestation WebGIS",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "python --version && python -c \"import sys; assert sys.version_info[:2] == (3, 13), f'Expected Python 3.13, got {sys.version}'\" && python -m pip install --upgrade pip && python -m pip install -r requirements.txt"
}
